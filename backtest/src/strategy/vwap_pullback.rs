//! VWAPPullback — port pendente (strategy 4 do sweep antigo).
//!
//! Logic atual em `backtest_sweep/src/main.rs::find_entries_pullback`.
//! Esta é a primeira strategy a ser portada (gate de equivalência inicial).

// TODO[milestone]: portar `find_entries_pullback` + grid de params
// (ema_period, max_trades_per_day, vwap_prox, confirm_bars, min_bars,
//  vwap_window, entry_window).
