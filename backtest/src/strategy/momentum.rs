//! Momentum strategies (RejShort/RejLong/MomShort/MomLong) — port pendente.
//!
//! Logic atual em `backtest_sweep/src/main.rs::find_entries` (strategies 0..3).

// TODO[milestone]: portar `find_entries` (strategy 0..3) do crate antigo.
// Gate de equivalência: rodar mesmo CSV no antigo e no novo, comparar TOP 30.
