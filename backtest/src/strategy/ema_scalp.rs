//! EMAScalp — port pendente (strategy 5 do sweep antigo).
//!
//! Logic atual em `backtest_sweep/src/main.rs::find_entries_ema_scalp`.

// TODO[milestone]: portar com grid (fast_period, slow_period, max_trades_per_day).
